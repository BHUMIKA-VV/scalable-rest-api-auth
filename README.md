# Scalable REST API with Auth

This repository contains a fullstack example: backend (Node/Express/Sequelize/Postgres) and frontend (React/Vite). Use the provided scripts to run, test, and package the assignment.
